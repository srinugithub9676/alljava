package com.resume.examples;

import java.util.List;

public class PagenationController {
	
	     //PAGENATION
	/*
	 * public void pageNation(Employee employee,Session session) { 
	 * List<Employee> list=servce.getAllEmployees(); List list2=list.subList(0, 3);
	 * session.setAttribute("list2",list);
	 * 
	 * }
	 *   FILE UPLOAD
	 *   
	 * @Autowired
	 * private MultipartFile multipartFile;
	 * 
	 * public ModelAndView upload(MultipartFile file)
	 * {
	 * File f=new File("E://");
	 * f.transeferTo(file);
	 * 
	 * 
	 * FILE DOWNLOADING
	 * 
	 * public void downlaodFile(HttpServletResponse response)
	 * {
	 * FileInputStream fis=new FileInputStream("test.xml")
	 * BufferOutputStream bos=new BufferOutputStream(response.getOutputstream();
	 * int data=fis.read();
	 * while(data=-1)
	 * {
	 * bof.write(data)
	 * data=fis.read();
	 * }}
	 * 
	 *  Server side Validations
	 *  ======================
	 *  
	 *  public class User
	 *  {
	 *  @NotNull
	 * private String name;
	 * 
	 * @Asserttrue
	 * private boolean working;
	 * 
	 * @size(min=10 max=20 message="")
	 * private String aboutme;
	 * 
	 * @min(value=10,message="")
	 * @max(value=150,message="")
	 * private int age;
	 * @Email
	 * private String email;
	 * 
	 */

}
